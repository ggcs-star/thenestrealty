CREATE TABLE loan_stages (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    created_at TIMESTAMP NULL DEFAULT NULL,
    updated_at TIMESTAMP NULL DEFAULT NULL
);




ALTER TABLE loans 
ADD loan_stage_id BIGINT UNSIGNED NULL;


ALTER TABLE loans
ADD CONSTRAINT fk_loan_stage
FOREIGN KEY (loan_stage_id)
REFERENCES loan_stages(id)
ON DELETE SET NULL;


ALTER TABLE loans 
DROP COLUMN loan_stage;


ALTER TABLE bookings
MODIFY unit_size INT NULL;




today


==============

ALTER TABLE loans 
ADD COLUMN customer_id BIGINT AFTER employee_id;

